# Research

Gear and product research from `/patina-gear`, filed by date as `YYYY-MM-DD-<topic>.md`.

Prices and models go out of date, so every report has a `refresh_after` date. Decisions are summarized in `kitchen/wishlist.md`.
